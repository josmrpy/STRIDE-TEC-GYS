# tarea de joel (diagrama de flujo a python)
# > Josimar Madrigal Espinoza - (STRIDE GYS)

base = float(input("Ingrese la base: "))
altu = float(input("Ingrese la altura: "))

sup = base * altu
per = 2 * (base + altu)

print(f"Superficie (SUP): {sup}")
print(f"Perímetro (PER): {per}")
